/// Comment processor for repeating table rows.
///
/// Implements the `repeatRow` directive, which duplicates a table row for each
/// element in a collection.
///
/// Ensures non-null values by default.
@NullMarked
package pro.verron.officestamper.preset.processors.repeatrow;

import org.jspecify.annotations.NullMarked;
