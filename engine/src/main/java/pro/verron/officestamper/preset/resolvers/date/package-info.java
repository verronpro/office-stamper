/// Resolver that formats [Date][java.util.Date] objects into strings.
///
/// Uses a configurable date format pattern to convert date values to their
/// string representation in the document.
///
/// Ensures non-null values by default.
@NullMarked
package pro.verron.officestamper.preset.resolvers.date;

import org.jspecify.annotations.NullMarked;
