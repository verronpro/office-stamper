/// Resolver that inserts images into the document.
///
/// Converts [Image][pro.verron.officestamper.preset.Image] objects returned by
/// SpEL expressions into inline images embedded in the resulting DOCX.
///
/// Ensures non-null values by default.
@NullMarked
package pro.verron.officestamper.preset.resolvers.image;

import org.jspecify.annotations.NullMarked;
